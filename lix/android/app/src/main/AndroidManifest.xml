<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ── Permissions ──────────────────────────────────── -->

    <!-- Internet: TMDB posters, iTunes API, Firebase -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Audio: 30s preview playback -->
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK"/>

    <!-- Network state: check connectivity -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <!-- ── Application ──────────────────────────────────── -->
    <application
        android:label="lix"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme"/>

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

        </activity>

        <!-- ✅ url_launcher support -->
        <activity
            android:name="com.url.launcher.UrlLauncherActivity"
            android:exported="true"/>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2"/>

    </application>

    <!-- ── Queries ──────────────────────────────────────── -->
    <queries>

        <!-- Text processing -->
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>

        <!-- ✅ Browser links (https / http) -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="http"/>
        </intent>

        <!-- ✅ Music streaming apps -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="spotify"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="deezer"/>
        </intent>

        <!-- ✅ Video streaming apps -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="netflix"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="primevideo"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="hotstar"/>
        </intent>

        <!-- ✅ YouTube app — trailers & songs -->
        <package android:name="com.google.android.youtube"/>

        <!-- ✅ Spotify app -->
        <package android:name="com.spotify.music"/>

        <!-- ✅ Netflix app -->
        <package android:name="com.netflix.mediaclient"/>

        <!-- ✅ Amazon Prime Video app -->
        <package android:name="com.amazon.avod.thirdpartyclient"/>

        <!-- ✅ Disney+ Hotstar app -->
        <package android:name="in.startv.hotstar"/>

        <!-- ✅ JioSaavn app -->
        <package android:name="com.jio.media.ondemand"/>

        <!-- ✅ Gaana app -->
        <package android:name="com.gaana"/>

        <!-- ✅ YouTube Music app -->
        <package android:name="com.google.android.apps.youtube.music"/>

        <!-- ✅ Amazon Music app -->
        <package android:name="com.amazon.mp3"/>

        <!-- ✅ Apple Music / iTunes (web fallback via browser) -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="music"/>
        </intent>

    </queries>

</manifest>
